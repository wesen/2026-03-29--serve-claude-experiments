# Tasks

## TODO

- [x] Implement companion manifest parsing, validation, and scanner/model integration
- [x] Create post-hoc manifest JSON files for every artifact currently in `imports/`
- [x] Surface manifest metadata in the first-pass UI and add a frontend-only search bundle with an autocomplete filter field
- [x] Expose manifest metadata in CLI/help docs, validate the feature, and record the detailed diary/changelog
